# 香辣卤味管理系统 — 验收测试清单

## 一、数据库 (7 项)

| # | 测试项 | 预期结果 | ✓ |
|---|--------|----------|---|
| 1.1 | 数据库 `SpicyBraiseDB` 存在 | `sqlcmd -Q "SELECT DB_NAME()"` 输出 SpicyBraiseDB | |
| 1.2 | 10 张表全部创建 | `SELECT COUNT(*) FROM sys.tables` ≥ 10 | |
| 1.3 | 每表有种子数据 | users≥5, products≥8, orders≥5, 其余≥5 | |
| 1.4 | 外键约束生效 | 删除被引用的分类应报错 | |
| 1.5 | CHECK 约束生效 | 插入辣度=6 的商品应报错 | |
| 1.6 | 软删除字段存在 | 所有业务表含 `is_deleted BIT` | |
| 1.7 | `usp_PlaceOrder` 存储过程可执行 | 调用下单返回 order_id | |

## 二、后端 API (12 项)

### 认证
| # | 测试项 | 方法 | 预期 | ✓ |
|---|--------|------|------|---|
| 2.1 | 注册新用户 | `POST /api/auth/register` | 200 + token | |
| 2.2 | 重复注册 | 同上 | 400 "用户名已存在" | |
| 2.3 | 登录 | `POST /api/auth/login` | 200 + token | |
| 2.4 | 错误密码 | 同上 | 401 "用户名或密码错误" | |
| 2.5 | 无 token 访问 | `GET /api/orders` | 401 | |

### 商品
| # | 测试项 | 方法 | 预期 | ✓ |
|---|--------|------|------|---|
| 2.6 | 商品列表 | `GET /api/products` | 200 + JSON 数组 | |
| 2.7 | 按分类筛选 | `GET /api/products?categoryId=1` | 仅返回该分类 | |
| 2.8 | 商品详情 | `GET /api/products/{id}` | 200 + 完整字段 | |
| 2.9 | 新增商品(admin) | `POST /api/products` + admin token | 200 | |
| 2.10 | 顾客新增商品 | `POST /api/products` + customer token | 403 | |

### 下单
| # | 测试项 | 方法 | 预期 | ✓ |
|---|--------|------|------|---|
| 2.11 | 正常下单 | `POST /api/orders` | 200 + orderId/orderNo | |
| 2.12 | 库存不足 | 订购超过库存的量 | 400 "库存不足" | |
| 2.13 | 优惠券+积分下单 | 带 couponId + usePoints | 正确计算折扣 | |
| 2.14 | 模拟支付 | `POST /api/payments/pay/{id}` | 200 + status=success | |
| 2.15 | 重复支付 | 再次模拟支付 | 400 "已支付" | |

## 三、前端页面 (10 项)

| # | 测试项 | 操作 | 预期 | ✓ |
|---|--------|------|------|---|
| 3.1 | 首页加载 | 打开 / | 19 个商品卡片 + 公告栏 | |
| 3.2 | 分类筛选 | 选「卤味素菜」 | 仅显示素菜商品 | |
| 3.3 | 商品详情 | 点击商品卡片 | 显示详情+评价+加购按钮 | |
| 3.4 | 登录页 | 点击登录 | 登录/注册 Tab 切换 | |
| 3.5 | 注册 | 填表注册 | 成功跳转首页 | |
| 3.6 | 加入购物车 | 点「加入购物车」 | 提示成功 + 购物车角标+1 | |
| 3.7 | 购物车管理 | 修改数量/删除 | 小计自动更新 | |
| 3.8 | 结算下单 | 选券+积分 → 提交 | 显示订单确认页 | |
| 3.9 | 模拟支付 | 点「模拟支付」 | 跳转订单列表 | |
| 3.10 | 公告栏 | 刷新页面 | 顶部显示折扣公告 | |

## 四、后台管理 (4 项)

| # | 测试项 | 操作 | 预期 | ✓ |
|---|--------|------|------|---|
| 4.1 | 管理员登录 | admin/test123 | 导航栏出现「后台管理」 | |
| 4.2 | 商品列表 | 进入后台管理 | 表格显示全部商品 | |
| 4.3 | 新增商品 | 填表提交 | 表格刷新，新商品出现 | |
| 4.4 | 删除商品 | 点删除 → 确认 | 商品从列表消失 | |

## 五、并发与安全 (5 项)

| # | 测试项 | 验证方式 | 预期 | ✓ |
|---|--------|----------|------|---|
| 5.1 | 库存不超卖 | 代码审查 `deductStock` | WHERE stock >= qty 原子更新 | |
| 5.2 | 优惠券不重复使用 | 代码审查 `lockCoupon` | WHERE status='unused' | |
| 5.3 | JWT 过期处理 | 等 24h 或用过期 token | 401 跳登录 | |
| 5.4 | 角色权限 | customer 访问管理后台 | 页面不可见 | |
| 5.5 | 密码加密 | 查数据库 password_hash | bcrypt $2a$ 开头 | |

## 六、容器化 (3 项)

| # | 测试项 | 操作 | 预期 | ✓ |
|---|--------|------|------|---|
| 6.1 | compose 启动 | `docker-compose up -d` | 4 个容器全部 healthy | |
| 6.2 | 前端访问 | http://localhost | 页面正常显示 | |
| 6.3 | 数据库初始化 | 查看 db-init 日志 | 迁移+seed 执行成功 | |

---

> 总计 41 项测试。全部通过即为验收合格。
